package com.example.sensor.model.virtual_sensor;

import com.example.sensor.model.SensorCategory;
import com.example.sensor.model.SensorType;
import com.example.sensor.model.TextArea;

public class VelocitySensor {
    private static final SensorCategory mSensorCategory=SensorCategory.VIRTUAL;
    private static final SensorType SENSOR_TYPE=SensorType.VELOCITY_SENSOR;
    public VelocitySensor(  TextArea textArea) {

    }

    //A implémenter
}
